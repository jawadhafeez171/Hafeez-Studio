{
  "name": "hafeezstudio",
  "version": "1.0.0",
  "description": "HafeezStudio website",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve ."
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
